# Public demo deployments can force framework Demo mode redaction.
# Demo mode redacts names, emails, and numbers in UI + agent results.
DEMO_MODE=false

# Required by hosted A2A async processing in production.
APP_URL=http://localhost:8106
